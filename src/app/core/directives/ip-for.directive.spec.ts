import { IpForDirective } from './ip-for.directive';

describe('IpForDirective', () => {
  it('should create an instance', () => {
    const directive = new IpForDirective();
    expect(directive).toBeTruthy();
  });
});
