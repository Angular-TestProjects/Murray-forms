import { IpIfDirective } from './ip-if.directive';

describe('IpIfDirective', () => {
  it('should create an instance', () => {
    const directive = new IpIfDirective();
    expect(directive).toBeTruthy();
  });
});
